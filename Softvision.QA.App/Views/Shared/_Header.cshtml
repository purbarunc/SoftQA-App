<div class="navbar navbar-default navbar-fixed-top so-header">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="@Url.Action("Index","Home")" style="font-weight:bold; font-size:25px">SoftQA</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <div class="navbar-btn">
                        @if (User.Identity.IsAuthenticated)
                        {
                            using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                            {
                                @Html.AntiForgeryToken()
                                <a class="btn btn-primary" href="javascript:document.getElementById('logoutForm').submit()">Sign out</a>
                            }
                        }
                        else
                        {
                            <a class="btn btn-primary" href="@Url.Action("Login","Account")">Login</a>
                        }
                    </div>
                </li>
            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li><a href="@Url.Action("Index","Home")">Questions</a></li>
                <li><a href="@Url.Action("Index","Profile")">Profile</a></li>
                @*<li><a href="#">Articles</a></li>
        <li><a href="#">Documents</a></li>*@
            </ul>
            @*<div class="col-sm-3 col-md-3">
                    <form class="navbar-form" role="search">
                        <div class="input-group">
                            <input class="form-control" placeholder="Search" name="q" type="text">
                            <div class="input-group-btn">
                                <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                            </div>
                        </div>
                    </form>
                </div>*@
        </div>
    </div>
</div>
